name: update
run-name: ${{ github.actor }}
on:
  push:
    branches:
      - master

jobs:
  commit:
    runs-on: ubuntu-latest
    steps:
    - name: Call for update
      run: |
        curl -X POST "https://api.daalbot.xyz/post/api/update?auth=${{ secrets.API_KEY }}"